# Notebook-specific requirements
# Base requirements are installed from ../shared/requirements-base.txt

# Jupyter
jupyter==1.0.0
notebook==7.0.6

# Note: Data science packages (scikit-learn, matplotlib, seaborn, scipy) removed as unused
# Add them back if needed for future ML/analysis work
